const { nanoid } = require("nanoid");

console.log("5 Unique IDs:");

for (let i = 1; i <= 5; i++) {
    console.log(`${i}. ${nanoid()}`);
}const { nanoid } = require("nanoid");

console.log("5 Unique IDs:");

for (let i = 1; i <= 5; i++) {
    console.log(`${i}. ${nanoid()}`);
}